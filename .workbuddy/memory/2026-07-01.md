# 2026-07-01

## FateTrigger 纹理与 Buffer 统计报告 HTML → MD 转换

- **任务**：将 `E:\AiDoc\FateTrigger_纹理与Buffer统计报告.html` 转为 `.md` 格式
- **输入**：HTML 单页报告，含 Chart.js 图表、表格、风险等级卡片
- **输出**：`E:\AiDoc\FateTrigger_纹理与Buffer统计报告.md`
- **关键处理**：
  - 7 个 Chart.js 图表全部转成 Markdown 表格（数据来源 labels + datasets）
  - 3 段风险卡片用 🟢🟡🔴 emoji 保留等级标识
  - 16 个 Pass 绑定表 + ASTC 实测表完整保留
  - 顶部统计卡片合并为一张汇总表
- **耗时**：1 个 Read + 1 个 Write，单次完成
- **备注**：原文件本身已在 `E:\AiDoc` 目录，无需再拷贝（用户工作目录规则：本就在 AiDoc 下的文件原地生成 .md 即可）

## 头部手游 PSO 方案汇总（横向技术专题 · 模式 B）

- **任务**：波哥让收集热门手游的 PSO 方案，用 `mobile-game-tech-summary` skill
- **判断模式**：技术点 + 跨多款游戏 → **模式 B**（横向专题汇总）
- **输出**：
  - 工作目录：`c:\Users\djangozhang\Desktop\MobileWorkSpace\.workbuddy\artifacts\头部手游PSO方案汇总.html`
  - 已自动同步到 `E:\AiDoc\头部手游PSO方案汇总.html`（40KB / 636 行）
- **主题色**：深紫 `#7e57c2` → 深蓝 `#42a5f5`（管线/状态意象）
- **章节骨架**（10 节齐全）：
  1. 核心结论速览：PSO 优化的三层心智模型（机制 / 资源 / 调度）
  2. 概念辨析：PSO / 变体 / Pipeline Cache 等 6 个易混概念
  3. 引擎机制对比：UE 手动 Caching + UE5 Precaching + Unity SVC
  4. 端游方案在移动端失效的 5 大原因（OpenGL ES 无 PSO / Metal 无 LRU / 后台编译关掉等）
  5. 标杆案例深拆：UE 移动端 PSO Caching 5 步流程 + Unity SVC 异步预热（米哈游系打法）
  6. 头部手游 PSO 实践横向对比（14 款，覆盖已知 14 款）
  7. 避坑清单：变体爆炸 / Metal 无 LRU / LogPSO 跑不全 / SVC 中断损坏缓存等 8 条
  8. 落地 Checklist 与定位工具（UE + Unity 工具集）
  9. 关键启示（对 URP / UE 移动端项目各 5 条）
  10. 参考资料（官方文档 / Meta Showdown Sample / 社区博客 / 玩家反馈反推）
- **关键资料**：
  - Epic UE 5.6 PSO Precaching 官方文档
  - Epic 为 Android 创建捆绑的 PSO 缓存（UE 5.3）
  - Meta Showdown Sample（UE 5.6 Quest 2/3，含 GeneratePSOCache.bat 完整流程）
  - Bearhammer Games · Quest 2 实战记录（AndroidEngine.ini 完整配置）
  - Unity URP 实战文章（变体管理：300 变体内 / Shader 编译 -65%）
  - 米游社区对"启动期着色器编译"的反馈
- **关键发现**：
  - UE5.3+ 的 PSO Precaching **仅支持 D3D12**，手游 Vulkan/Metal/GLES 暂时用不上
  - Metal 的 MTLPipelineCache **无 LRU 机制**，iOS 项目必须手动管理
  - Vulkan PipelineCache 跨设备/驱动版本会失效
  - 10000 变体 ≈ 500MB / 20s 移动端加载，全量打包会让低端机 OOM
  - 米游社区"启动卡顿"反馈的根因是 SVC 异步编译被中断生成损坏缓存
- **模式 B 的复用经验**：
  - 模式 B 的"标杆案例深拆 + 横向对比表"是骨架，缺一不可
  - 主题色要与"技术语义"匹配（这次用深紫→深蓝对应"管线/状态"）
  - 玩家社区反馈是反向证据（"启动卡顿"反推 SVC 在跑）
  - "端游方案为何在移动端失效"是模式 B 的核心价值点，必须主动写

## 头部手游画质分级方案汇总（横向技术专题 · 模式 B）

- **任务**：波哥让收集热门手游的画质分级方案（每档效果 + 数值 + 适配机型），自由发挥，用 `mobile-game-tech-summary` skill
- **判断模式**：技术主题 + 跨多款游戏 → **模式 B**
- **输出**：
  - 工作目录：`c:\Users\djangozhang\Desktop\MobileWorkSpace\.workbuddy\artifacts\头部手游画质分级方案汇总.html`
  - 已自动同步到 `E:\AiDoc\头部手游画质分级方案汇总.html`（36KB）
- **主题色**：青绿 `#4ad8a8` → 深紫 `#7c5fff`（"档位滑块"语义、技术感）
- **章节骨架**（10 节齐全）：
  1. 核心结论：分级方案统一心智模型（档位数 + 引擎机制 + 数值轴）
  2. 概念辨析：画质 / 帧率 / 分辨率 / 特效 4 维度解耦（含 4 条误区）
  3. 引擎机制：UE Scalability + DeviceProfile 3 基线 + Unity URP QualitySettings + Adaptive Performance + 跨端 DRS/FSR/VRS
  4. 八大分级维度拆解：渲染精度/阴影/AA/后处理/特效/植被/帧率/DRS 的典型范围与 GPU 权重
  5. 头部手游分档横向对比表（14 款游戏大表）
  6. 标杆深拆 · 永劫无间 6 档分级（流畅/标准/高清/精致/极致/终极）
  7. 机型映射：入门→中端→高端→旗舰 4 梯队路由
  8. 避坑清单：阴影影响可见度/AA 在低分辨率浪费/HDR 伪开/弱散热反向劣化/移动端"光追"是部分效果/插帧≠真高帧率
  9. 落地 Checklist + 决策树 + 性能定位工具
  10. 参考资料（GDC + 引擎文档 + 官方公告 + 技术博客 + 玩家实测）
- **横向对比覆盖**（14 款）：王者荣耀、原神、星铁、绝区零、鸣潮、光遇、燕云、永劫、和平精英、CODM、三角洲、暗区突围、蛋仔、第五人格
- **关键发现**：
  - 档位数与品类强相关：FPS 5~6 档，MMO 4~5 档，UGC 3~4 档，体验向 2~3 档
  - 永劫无间 6 档 = **4 档可用 + 1 档"质变档" + 1 档"端游专属档"** 的非线性分布
  - 鸣潮 3.2 新增"超高画质"独占骁龙 8 Elite / 8s Gen 4 / 天玑 9400+ / 玄戒 O1
  - 和平精英"超高清"强制关闭 AA + 锁帧（高档反而比 HDR 高清限制多）
  - 暗区突围"光线追踪"实为 Vulkan Ray Query 反射（部分光追），不是完整光追
  - 王者 HDR 与"极致"绑定同步开启，HDR 实际是"炫彩管线替换"而非真 HDR
  - 光遇 3 格高清需机型适配，部分骁龙 8 系反而开不了（未在适配清单）
  - "插帧"≠"原生高帧率"，鸣潮 90~120 帧是插帧实现，有延迟
- **资料充足度**：★ 资料公开多（玩家侧文章丰富），管线侧参数仍稀缺
- **避坑点**：
  - 公开文章多描述"风格差异"，具体贴图分辨率/SceneColor 格式/Pass 顺序极少公开
  - 数字必须用范围或"约"，不准伪精确
  - "光追""HDR"等宣传词不能照抄，需说明实际技术范围
- **参考资料**：
  - UE 5.1 DeviceProfile 官方文档
  - UE 5.6 移动端 Rendering Features 文档
  - 永劫无间官方公测定档公告
  - 鸣潮 3.2 官方更新公告
  - 王者荣耀画质升级公告（2020-12）
  - 火山引擎《风格锚点+动态适配：Unity 跨设备渲染》
  - Wayline Blog · Adaptive Resolution Scaling
  - 腾讯 PerfDog 绝区零画质测试
  - 18183 / B 站 / 虎牙玩家实测
