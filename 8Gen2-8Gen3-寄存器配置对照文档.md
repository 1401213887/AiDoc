# 8Gen2 vs 8Gen3 寄存器配置对照文档

# 骁龙 8Gen2 vs 8Gen3 寄存器配置对照文档

Adreno 740 vs Adreno 750 — 寄存器机制 · 高 GPR 行为差异 · AOC 指标速查

**对象**　移动端 GPU shader 优化
**工具**　Snapdragon Profiler / Adreno Offline Compiler
**更新**　2026-06-29

## 1硬件规格对照

| 项目 | 8Gen2 · Adreno 740 | 8Gen3 · Adreno 750 |
| --- | --- | --- |
| 发布时间 | 2022-11 | 2023-10 |
| ALU / Shader 通道 | 1536 | 1536 数量未变 |
| GPU 频率 | ~680–719 MHz | ~903–1000 MHz +约30% |
| FP16 峰值算力 | ~4.42 TFLOPS | ~5.53 TFLOPS ×1.25 |
| L2 / L3 缓存 | 1MB / 3MB shared | 1MB / 更大 L3 增大 |
| 显存带宽 | 67.2 GB/s (LPDDR5X 8.4GT/s) | 76.8 GB/s (LPDDR5X 9.6GT/s) +14% |
| 制程 | TSMC N4 | TSMC N4P |
| API | OpenGL ES 3.2 / Vulkan 1.3 | OpenGL ES 3.2 / Vulkan 1.3 |
| 每 SP 寄存器文件容量 | 官方未公开　高通从未披露 740/750 的寄存器文件物理大小，无可靠实测公开数据 | |

**⚠️ 重要前提：**关于"8Gen3 寄存器比 8Gen2 多多少字节、能多跑几个 wave"这类**确切数字无法获取**，本文档不做臆测。下文分析建立在两者**同属 Adreno 700 系列、共用同一套 SP 微架构**这一确定事实之上。

## 2寄存器(GPR)→ occupancy 换算机制

这套机制在 740 和 750 上**完全相同**，是理解高 GPR 影响的核心：

寄存器文件容量(固定)÷
单 wave GPR 占用=
可同时活跃的 wave 数→
occupancy→
延迟隐藏 / 吞吐

| 概念 | 说明 |
| --- | --- |
| fiber | 最小执行单位，对应一个工作项 / 一个像素或顶点 |
| wave | 一组锁步执行的 fiber，Adreno 波大小常见 8/16/32/64/128，由 GPU 系列与编译器决定 |
| 全精度寄存器 | 128bit，可存 4 个 FP32 **或** 8 个 FP16 |
| 半精度寄存器 | 64bit，可存 4 个 FP16，占用按"半个"计 |
| 活跃 wave 数 | 一个 SP 内最多可并行的 wave 数；受寄存器占用 + 寄存器文件大小限制，Adreno 通常上限 16 波 |

**核心因果链：**GPR 数值高 → 单 wave 占用大 → 活跃 wave 变少 → occupancy 掉 → 访存延迟无法被其他 wave 填补 → ALU 空转 (stall)。占用极端超标时还会触发 **register spill**(寄存器溢出到显存)，代价更大。

## 3GPR 数值高时，8Gen2 与 8Gen3 的差异

### ① 寄存器行为本身：高度一致

同代架构、同样的 128bit 全精度寄存器粒度。**同一个 shader、同样的 GPR 数值，在两者上换算出的活跃 wave 数大概率相同**。GPR 拉高导致 occupancy 下跌的"拐点"位置基本一致——这一层**看不出明显区别**。

### ② 真正拉开差距的是"掉了 occupancy 之后谁更扛"

| 低 occupancy 场景 | 8Gen2 (Adreno 740) | 8Gen3 (Adreno 750) |
| --- | --- | --- |
| 单 wave 推进速度 | 受限于较低频率 | 频率高 ~30%，stall 绝对时间更短 |
| 访存往返代价 | L3 较小，更易 cache miss | L3 更大 + 带宽 +14%，访存更省 |
| 综合表现 | occupancy 不足的惩罚更明显 | 同样掉 occupancy，但"惩罚更轻" |

现象总结：**同一个高 GPR shader，两者都会掉 occupancy，但 8Gen3 的性能损失更小**——靠的是频率和缓存，不是寄存器更多。

### ③ register spill 临界点

GPR 高到溢出显存时，8Gen3 更大的缓存和更高带宽能稍缓解 spill 的访存代价。但**溢出本身要尽力避免**，靠硬件兜底是下策。

## 4AOC 关键寄存器指标速查

用 Adreno Offline Compiler 分析 shader 时，重点盯这几个值：

| 指标 | 含义 | 怎么看 |
| --- | --- | --- |
| 全精度寄存器占用 (每着色器实例) | 使用的 128bit 寄存器数量 | 越低越好；这是 occupancy 的主要决定因素 |
| 半精度寄存器占用 | 使用的 64bit 寄存器数量 | 多用 FP16 可把全精度需求挪到这里 |
| 总寄存器占用 | 综合 128bit 等效占用 | 过高 → 活跃 wave 减少 / 可能溢出 |
| ALU 波阵列占用 % | shader 最大 ALU wave occupancy | **最直接的 occupancy 指标**，比盯 GPR 绝对值更有用；值低 = shader 表现差 |
| 指令数 / 纹理获取数 | ALU 指令、纹理与内存读取计数 | 辅助判断瓶颈在算力还是访存 |

## 5实操优化建议

#### 降低 GPR 占用的手段

- 多用 `mediump` / FP16 half 精度变量
- 缩小向量、避免超大动态向量
- 把变量定义**挪近使用点**，缩短活跃区间
- 用数组替代大量独立变量；尽量用静态常量赋值
- 合并可分组的纹理获取(每组 <15)

#### 调优策略

- **不要为单颗芯片单独调寄存器**——两者行为一致，一次优化两边受益
- **优先用 8Gen2 调**：频率低、缓存小，occupancy 不足惩罚更明显；在它上面达标，到 8Gen3 余量更足
- 盯 **ALU 波阵列占用 %** 而非 GPR 绝对值
- 真机测试 ≥10 分钟，观察热降频下的表现

**一句话结论：**8Gen2 与 8Gen3 的寄存器配置/行为基本一致，GPR 高对两者的"机制影响相同"；区别在于 **8Gen3 靠更高频率 + 更大缓存，让低 occupancy 的性能惩罚更轻**。优化应针对寄存器占用本身下手，并以 8Gen2 为基准调，而非依赖任一芯片的硬件余量。

本文档基于公开规格与 Adreno 架构文档整理；寄存器文件物理容量为高通未公开项，相关数字均未臆测。
