# 鸣潮 移动端渲染技术要点总结

# 鸣潮 · 移动端渲染技术要点总结

UE4 自定义 · 开放世界 ARPG · NPR + 部分 PBR 混合 · 动作系统 · 自研 TAAU

**引擎**UE4.27 深度定制（Kuro Engine 改造）

**风格**NPR + PBR 混合

**分辨率**720P~1080P 自适应

**API**Vulkan / Metal

**跨平台**iOS / Android / PC / PS5

## 一、核心结论速览

#### 渲染策略

- 移动端 **UE4 Mobile Forward** 改造
- 角色 NPR + 场景偏 PBR 的混合体系
- 大量开放世界元素：水、植被、地形精度对标 PC

#### 核心技术

- 角色：双 Ramp + Anisotropic 头发
- 场景：Triplanar + 多层 Splat + Decal
- 水体：屏幕空间反射 + Caustics + Foam

#### 预算/Format

- SceneColor `R11G11B10`
- Shadow 2 级 CSM `1024²`
- 移动端禁用 Lumen / Nanite

鸣潮的工程画风

鸣潮是少数**主流 NPR 二次元用 UE 而非 Unity** 的产品。这给它带来：① 渲染管线与场景流送对标 PC 主机；② 但同时 UE 移动端的 NPR 工业化基础不如 Unity，需要额外工程投入。

## 二、UE 改造与移动管线

为什么选 UE

库洛技术栈延续自《战双帕弥什》，对 UE 的渲染层有较深定制经验。UE 的 RDG（Render Graph）+ MobileForward 路径更适合开放世界，便于做 streaming 与 LOD。

| UE 模块 | 鸣潮改造点 |
| --- | --- |
| MobileBasePass | 替换 NPR 光照模型，加入 Ramp + 描边 |
| Material Editor | 支持 NPR 节点（Anisotropic Hair、Face SDF 等） |
| Light System | 移动端关闭 Movable PointLight，改静态/Stationary |
| Shadow | 2 级 CSM，禁用 Distance Field Shadow |
| PostProcess | 移除 SSAO/SSR/AutoExposure 默认实现，自研轻量替代 |
| RDG | 对 Mobile Forward 路径精简，合并部分 Render Pass 减少切换 |

### RDG / MobileBasePass 改造深度分析

为什么"性价比最高"

UE4 Mobile Forward 是**为 PBR 光照设计的固定管线**，每个 Pass 硬编码为独立渲染阶段。这带来两个核心问题：① Pass 切换在 Tile-Based GPU（Mali / Adreno / Apple GPU）上代价极高——每次 RenderPass 边界都触发 Tile 内存回写主存再读回；② 默认 BasePass shader 走 `FMobileShadingModel` 的 PBR 分支，NPR 所需的 Ramp 采样、SDF 面部光照、Anisotropic 高光没有入口。改造管线框架一次投入，收益覆盖所有场景。

#### 改造 1：Pass 合并——减少 RenderPass 边界

UE4 Mobile Forward 默认 Pass 切换开销：

- **RenderTarget 绑定切换**：Vulkan/Metal 上是 RenderPass 开始/结束，触发 Tile 刷新
- **Pipeline State 切换**：shader、blend state、depth state 全量重绑
- **GPU 空泡**：Pass 间 GPU 可能 stall，等待上一个 Pass 的 Tile Resolve 完成

库洛的改造核心：NPR 角色和 PBR 场景放在**同一个 BasePass**里，通过 `Material Domain` 区分走哪个 shader 分支。这样：

- 不需要为角色单独开一个 Pass（UE 默认的 Translucency / Custom Depth 分离写法）
- 一个 RenderPass 内完成所有不透明物体渲染，Tile 内存常驻，不需要中途回写主存
- 描边（Outline）单独成 Pass 是因为需要完整的 Depth Buffer 做背面剔除，无法与 BasePass 合并

#### 改造 2：MobileBasePass shader 改造——NPR 光照入口

在 `FMobileShadingModel` 中插入自定义分支，使 NPR 与 PBR 在同一 Draw Call 池中按材质排序绘制：

```
// 伪代码示意
if (ShadingModel == MSM_CustomNPR)
{
    // Ramp 采样（双 Ramp：暗部分界 + 暗部内部细分）
    half3 baseColor   = Ramp1.Sample(NdotL);
    half3 shadowColor = Ramp2.Sample(NdotL);

    // Material ID 区分（皮肤/布料/金属选不同 Ramp）
    half3 final = SelectRampByMaterialID(MaterialID, baseColor, shadowColor);

    // 描边用单独 Pass，这里只写 BasePass 输出
    return final;
}
else
{
    // 走默认 PBR 路径
    return StandardPBR(N, V, L, Roughness, Metallic, BaseColor);
}
```

#### 改造 3：RDG 精简——砍掉 Mobile 不需要的 Pass

UE4 默认 RDG 会为 Mobile 路径插入"保险"Pass，很多在移动端是浪费的：

- **移除 SSAO / SSR / AutoExposure 默认 Pass**，自研轻量替代，高档按需插入
- **Translucency 不走 SeparateTranslucency**——UE4 默认把半透明渲染到独立 RT 再合成，多一次全屏 Blit；水/玻璃/特效直接在同 RT 渲染，用排序代替分离
- **合并 PostProcess 链**——Bloom + Tonemap + ColorGrading 压到一个 Compute / Fragment Pass 里，避免多次全屏采样

#### 改造收益对比

| 优化方向 | 投入 | 收益 | 性价比 |
| --- | --- | --- | --- |
| **RDG / Pass 合并** | 中（改管线框架） | 省掉 2-3 次 Tile 回写，GPU 利用率提升 10-15% | 最高 |
| LOD / Streaming | 低 | 减少绘制量 | 中 |
| 材质简化 | 低 | 省 ALU | 中 |
| 纹理压缩 | 低 | 省带宽 | 低（效果线性） |

Pass 合并直接减少的是 **GPU 空泡和带宽浪费**——不是因为画太多而慢，而是因为切换太多而闲着等。这是移动端最大的隐形杀手。

## 三、Pass 顺序与 RT 布局

#### Pass 1 · ShadowMap

2 级 CSM，1024²。移动端高档开 RVT shadow（仅地面静态阴影），免去每帧绘制大场景投影。

#### Pass 2 · Depth Prepass

角色 + 大型建筑写 Z，便于后续 Soft Particle / Decal Z-test。

#### Pass 3 · BasePass

地形 → 植被 → 建筑 → 装饰 → 角色，按距离与 LOD 分桶。NPR 与 PBR 通过 Material Domain 区分，使用同一 Pass 但不同 shader。

#### Pass 4 · Outline

背面外扩描边，距离衰减。低档可关闭。

#### Pass 5 · Atmosphere / Fog

体积雾用屏幕空间近似，仅高档启用。

#### Pass 6 · Translucency

水、玻璃、技能特效。水体最贵，常单独走 Pass 以便后处理 mask。

#### Pass 7 · PostProcess

Bloom + Tonemap + Color Grading。移动端默认关 SSAO/SSR。

## 四、角色 NPR 与高光带

### 双 Ramp 策略

- **Ramp 1**：基础阴影分界（暗部颜色）
- **Ramp 2**：暗部内部细分（次表面散射近似）
- 结合 Material ID（皮肤/布料/金属）选择不同 Ramp

### 头发 Anisotropic

- 切线沿头发方向（vertex tangent）
- 2 层高光：底层白色，顶层角色专属调色
- 用噪声贴图调制高光位置，避免"塑料感"
- 高档加 **头发自阴影 RT**，低档关

### 面部

- SDF 面部贴图（与原神同思路）
- 瞳孔单独 Mesh，加视差偏移以模拟立体感
- 嘴部高光用 Mask 控制，避免普通 NdotL 时嘴唇反光过强

## 五、开放世界场景

#### 地形

- 4~6 层 Splat + Triplanar 砾石
- 移动端用 SVT 压缩多层贴图带宽
- 地表 Decal 大量使用（脚印、痕迹、苔藓）

#### 植被

- 大量 GPU Instancing
- 风场模拟：CPU 高度 + Vertex 偏移
- 远景树 Imposter

#### 水体

- 主流水面用 **SSR**（高档）/ Cubemap（低档）
- Caustics 焦散贴图 UV 滚动
- 水边 Foam 通过 Depth Buffer 差值检测

#### 天空 / 大气

- Sky Atmosphere（UE 自带）+ 自定义云层
- Volumetric Cloud 仅 PC，移动端用贴图近似

## 六、动作战斗特效

鸣潮战斗的视觉重点

连段切换/暴击/QTE 反应快，需要"**瞬间炸裂的特效**"，但又不能像绝区零那样满屏 Halftone（产品定位是写实风偏 NPR）。

- **武器轨迹**：Mesh Trail + 沿轨迹采样 Emissive 贴图
- **命中冲击**：屏幕空间径向波浪 + Chromatic Aberration（瞬间）
- **共鸣释放**：切相机 + 电影级 DOF + Bloom 暴增
- **属性变奏**：火/冰/雷/气/物理/光/暗，每属性独立调色 LUT

## 七、后处理与色调

| 后处理 | 低档 | 中档 | 高档 |
| --- | --- | --- | --- |
| Bloom | 关 | 3 级 | 5 级 |
| Tonemap | 简化 ACES | ACES | ACES + LUT |
| SSAO | 关 | 关 | 开（可选） |
| SSR | 关 | 关（水用 Cubemap） | 开（仅水/玻璃） |
| DOF | 关 | 过场开 | 战斗开 |
| Volumetric Fog | 关 | 关 | 开（轻量） |

### AA 方案：自研 TAAU（延迟渲染后处理抗锯齿）

移动端坚持 Deferred（同屏 200+ 动态光源 / SSR / GTAO），**无法用 MSAA**，AA 走自研 **TAAU**（时间性抗锯齿 + 上采样，TAA 升级版）。二次元 NPR 屏幕空间边缘像素占比 4%~5%（写实 PBR 仅 1%~2%），对 AA 要求极高。

**管线改造**

- 额外增加 **Velocity Pass**：速度编码进 24 位 RGB + Character Mask（角色遮罩）
- TAA Pass 从默认位置移到 **Bloom + ToneMapping 之后**，升级为 TAAU

**移动端鬼影优化（带宽权衡）**

| 项 | 做法 |
|---|---|
| 采样 | PC 9 点 → 移动端**十字星 5-tap cross** 省带宽 |
| History Clamp | 最廉价 **AABB 包围盒**限制历史颜色范围 |
| Clamp 颜色空间 | 高配 **YCoCg**（亮度感知更准）/ 低配 **RGB**（省色彩转换） |

**角色边缘闪烁优化**（勾边 Pass 因成本过高：BasePass×2 / Velocity×2 / Shadow / 蒙皮×2，去掉了 Velocity 渲染，导致头发/服饰边缘无法回溯历史帧 → 闪烁严重）

1. **动静分离权重**：速度越大当前帧权重越高（抑鬼影），速度越小历史帧权重越高（稳画面）
2. **低通滤波**：对闪烁边缘区域用已有 5-tap 数据简单模糊压制
3. **动态锐化**：对被 TAA 糊掉的动态物体用 **Unsharp Mask** 局部锐化（复用 5-tap 数据，不额外采样）

**上采样**：实现类似 **FSR 1.0 的多项式逼近**（如 Lanczos2），除距离外还考虑边缘信息，优于双线性。

## 八、移动端性能策略

### 分档

- 低/中/高/极高，旗舰机推 60/120 fps 双档
- 低档关 SSR/SSAO/DOF/体积雾，关 Caustics，关 Imposter→直接消失
- 极高档对标 PC 中等画质

### 开放世界 streaming

- UE 的 World Composition + Level Streaming
- 分块 1km × 1km，前后 3 块预加载
- 纹理 Mip Streaming，距离越远越高 Mip

### UE 移动端坑点

- Mobile Forward 默认对 RDG 支持不佳，库洛改造了 Pass 合并
- Vulkan 上 PreRotation 必须开，否则竖屏物理朝向会引入 Blit
- 动态光源在移动端代价高，控制场景内 Movable Light ≤ 4

## 九、关键启示

#### 对 UE 系 NPR 项目

1. UE 移动端 NPR 需要自己撸基础设施，不能指望引擎给
2. 双 Ramp 比单 Ramp 自由度更高，对暗部细节更可控
3. RDG / MobileBasePass 改造是性价比最高的优化点

#### 对开放世界项目

1. 水体的 SSR 性价比有限，Cubemap 在低档够用
2. Imposter 是远景植被必备
3. SVT 在 UE 中是地形多层 Splat 的关键，否则 sample 数爆炸

## 十、参考资料

- 库洛 GDC 公开演讲（鸣潮制作分享）
- 战双帕弥什前作技术分享（库洛 NPR 技术演进）
- 个人截帧（设备：iQOO 12 / iPhone 15 Pro）
- 知乎技术博客：鸣潮 NPR 复刻
- UFSH2023《鸣潮》基于 UE4 的多平台效果与性能优化实践（王宏波）：https://zhuanlan.zhihu.com/p/678876237

鸣潮移动端技术要点总结 · 内部研究文档 · 信息基于公开演讲与截帧分析整理
