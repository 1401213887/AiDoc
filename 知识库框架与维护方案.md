# AiDoc 知识库框架与维护方案

> 本文档描述 `E:\AiDoc` 本地知识库的当前架构、各层职责、日常使用方式，以及新增/修改文档后的维护流程。  
> 最后更新：2026-07-06

---

## 一、总体架构

本知识库采用**两层分工、同一数据源**的设计：所有文档存放在 `E:\AiDoc`，工具各取所需。

```
E:\AiDoc（文档源）
    │
    ├── generate_index.py ──→ README.md（分类导航首页）
    │
    └── Obsidian（vault）───→ 浏览 / 渲染 / 全文搜索 / 双链
```

| 层 | 工具 | 作用 | 是否需联网 |
|----|------|------|-----------|
| 目录导航 | `generate_index.py` | 扫描文档，按规则归类，生成 `README.md` | 否 |
| 浏览 / 检索 | Obsidian | Markdown 渲染、全文搜索（`Ctrl+Shift+F`）、文档跳转 | 否 |

**AI 问答（可选）：** 在 Cursor 中打开 `E:\AiDoc`，通过 `@` 引用文件或目录与 AI 对话，无需额外安装 RAG 工具。

**设计原则：**

- 文档即数据，不移动、不改名，全部用相对路径互链。
- Git 只备份文档与脚本；工具缓存、个人配置不入库（见 `.gitignore`）。
- 日常维护一键化：新增文档后运行 `kb.ps1` 刷新导航即可。

---

## 二、各组件说明

### 2.1 文档源 · `E:\AiDoc`

知识库根目录，包含 UE 移动端渲染相关的 `.md`、`.html`、`.pdf`、`.docx` 等文件，以及子目录（如 `MobileRenderPath/`）。

**支持格式：**

| 格式 | 导航索引 | Obsidian |
|------|---------|----------|
| `.md` | ✅ | ✅ 最佳 |
| `.html` | ✅ | 外链浏览器打开 |
| `.pdf` / `.docx` | ✅ | 有限 |
| `.csv` / `.xlsx` / `.pptx` | ✅ | — |

### 2.2 导航生成 · `generate_index.py`

- **作用**：扫描目录下所有有效文档，按关键词规则归入 8 个类目，提取标题与修改时间，自动生成 `README.md` 总导航。
- **规则**：文件不移动，仅用相对路径链接；`_archive_old/`、`.git/` 等目录自动忽略。
- **用法**：
  ```powershell
  cd E:\AiDoc
  python generate_index.py
  ```

### 2.3 浏览与全文搜索 · Obsidian

- **安装**：开始菜单搜索 Obsidian，或运行 `_setup\Obsidian-1.12.7.exe`（若尚未安装）。
- **打开方式**：Obsidian → `Open folder as vault` → 选择 `E:\AiDoc`。
- **首页**：打开 `README.md`，点击分类链接跳转各文档。
- **全文搜索**：左侧放大镜，或 `Ctrl+Shift+F`。
- **说明**：Obsidian 会自动感知目录内新增/修改的 `.md` 文件，**无需额外同步操作**。

个人配置目录 `.obsidian/` 已在 `.gitignore` 中忽略，不入 Git。

### 2.4 一键维护脚本 · `kb.ps1`

```powershell
cd E:\AiDoc
powershell -ExecutionPolicy Bypass -File .\kb.ps1
```

运行 `generate_index.py` 刷新 `README.md` 导航。

### 2.5 AI 问答 · Cursor（推荐）

在 Cursor 中打开 `E:\AiDoc` 作为工作区，即可：

- 用 `@文件名` 或 `@文件夹` 将文档作为上下文
- 直接提问，例如「TBDR 片上缓存优化的核心思路是什么？」
- 无需本地向量库或额外嵌入步骤

适合开发者日常查阅与追问；无 AnythingLLM 式的统一 Citations UI，但维护成本最低。

---

## 三、日常维护流程

### 3.1 新增文档

```
① 将文件放入 E:\AiDoc（或子目录）
        ↓
② 运行 kb.ps1（刷新 README.md 导航）
        ↓
③ （可选）git commit 备份
```

**Obsidian 侧**：打开 vault 即可看到新文件，无需操作。

### 3.2 修改已有文档

| 变更类型 | 需要做的 |
|---------|---------|
| 修改 `.md` 正文 | Obsidian 自动更新；若改了文件名，建议跑 `kb.ps1` 更新导航链接 |
| 修改文件名或路径 | 运行 `kb.ps1` 更新导航 |

### 3.3 删除文档

1. 从 `E:\AiDoc` 删除文件。
2. 运行 `kb.ps1` 更新导航。

### 3.4 Git 备份（可选）

```powershell
cd E:\AiDoc
git add -A
git commit -m "docs: 新增/更新 xxx"
```

`.gitignore` 已排除工具缓存与个人配置，提交内容仅为文档与脚本。

---

## 四、快速参考

### 一句话记忆

> **新文档 → 跑 `kb.ps1`**

### 常用路径

| 用途 | 路径 |
|------|------|
| 文档根目录 | `E:\AiDoc` |
| 导航首页 | `E:\AiDoc\README.md` |
| 维护脚本 | `E:\AiDoc\kb.ps1` |
| 导航生成 | `E:\AiDoc\generate_index.py` |
| Obsidian vault | `E:\AiDoc`（整个目录） |

### 常见问题

**Q：`.html` 文件怎么查看？**  
A：Obsidian 不内嵌渲染 html，右键 → 用浏览器打开即可。

**Q：想用 AI 问知识库里的内容？**  
A：在 Cursor 中打开 `E:\AiDoc`，用 `@` 引用相关文档后提问。

---

## 五、目录与 Git 约定

```
E:\AiDoc\
├── README.md                  ← 自动生成的导航首页（勿手改，改完会被覆盖）
├── generate_index.py          ← 导航生成脚本
├── kb.ps1                     ← 一键维护脚本
├── 知识库框架与维护方案.md     ← 本文档
├── .gitignore                 ← 排除工具缓存与个人配置
├── _setup/                    ← 安装包（已 gitignore，不入库）
├── .obsidian/                 ← Obsidian 个人配置（已 gitignore）
├── MobileRenderPath/          ← 子目录文档
└── …（各类技术文档）
```

**不入 Git 的内容：**

- `.obsidian/`、`.trash/`、`*.base` — Obsidian 个人配置与 Bases 数据库
- `_setup/` — 安装包

---

## 六、架构演进记录

| 阶段 | AI 问答方案 | 说明 |
|------|------------|------|
| 初版 | AnythingLLM + Ollama | 本地 RAG，GPU 占用较高 |
| 中期 | AnythingLLM + DeepSeek | 对话走云 API，嵌入本地 CPU |
| **当前** | Cursor `@` 引用 | 两层架构（导航 + Obsidian），无本地 RAG 工具 |

AnythingLLM 与 Ollama 已于 2026-07-06 卸载移除。

---

## 七、后续可选扩展

- **静态站点发布**：用 MkDocs Material 生成可分享的 HTML 站点（`pip install mkdocs-material && mkdocs serve`）。
- **恢复本地 RAG**：若将来需要离线 AI 问答，可重新评估 LlamaIndex + Chroma 等轻量方案。

如需实施以上扩展，可在 Cursor 中提出具体需求。
